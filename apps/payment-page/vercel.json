{
  "buildCommand": "npm run set-env && npm run build",
  "outputDirectory": "dist/payment-page/browser",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
